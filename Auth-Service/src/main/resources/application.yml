server:
    port: 8082
    servlet:
        context-path: /auth-service

spring:
    application:
        name: auth-service

eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8081/eureka